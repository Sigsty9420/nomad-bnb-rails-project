<%= @reservations.each_with_index do |reservation, index| %>
  <h3>Reservation #<%= index + 1 %> - Check in: <%=reservation.start_date.to_date%> / Check out: <%=reservation.end_date.to_date%></h3>
<% end %>
